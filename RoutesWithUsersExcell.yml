info:
  name: RoutesWithUsersExcell
  type: http
  seq: 14

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/RoutesWithUsersExcell"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: RouteId
      value: "3"
      disabled: true
    - name: SupplierId
      value: ""
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
