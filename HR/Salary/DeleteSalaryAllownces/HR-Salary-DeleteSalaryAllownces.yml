info:
  name: /HR/Salary/DeleteSalaryAllownces
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseCoreUrl}}/HR/Salary/DeleteSalaryAllownces"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: Periti
  body:
    type: multipart-form
    data:
      - name: SalaryAllowncesId
        type: text
        value: "8"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
