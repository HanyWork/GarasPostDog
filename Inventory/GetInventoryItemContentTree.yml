info:
  name: GetInventoryItemContentTree
  type: http
  seq: 50

http:
  method: GET
  url: "{{BaseCoreUrl}}/Inventory/InventoryItem/GetInventoryItemContentTree"
  headers:
    - name: CompanyName
      value: garastest
    - name: UserToken
      value: "{{UserToken}}"
    - name: InventoryItemId
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
