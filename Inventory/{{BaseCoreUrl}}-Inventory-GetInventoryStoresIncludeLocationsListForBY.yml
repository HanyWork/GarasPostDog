info:
  name: "{{BaseCoreUrl}}/Inventory/GetInventoryStoresIncludeLocationsListForBY"
  type: http
  seq: 40

http:
  method: GET
  url: "{{BaseCoreUrl}}/Inventory/InventoryItem/GetInventoryStoresIncludeLocationsList"
  headers:
    - name: CompanyName
      value: garastest
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
