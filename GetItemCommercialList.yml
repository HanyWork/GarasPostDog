info:
  name: GetItemCommercialList
  type: http
  seq: 46

http:
  method: GET
  url: "{{libmarkapiBaseUrl}}/InventoryMatrialReleaseForBY/GetItemCommercialList"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: "{{CompanyName}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
