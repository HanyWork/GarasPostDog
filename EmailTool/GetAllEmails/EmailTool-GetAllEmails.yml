info:
  name: /EmailTool/GetAllEmails
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/EmailTool/GetAllEmails"
  headers:
    - name: CompanyName
      value: marinaplt
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
