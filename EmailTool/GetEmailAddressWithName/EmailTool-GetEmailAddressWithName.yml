info:
  name: /EmailTool/GetEmailAddressWithName
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/EmailTool/GetEmailAddressWithName"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: stmark
    - name: SearchKey
      value: ""
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
