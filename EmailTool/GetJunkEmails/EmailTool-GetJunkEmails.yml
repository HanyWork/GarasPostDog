info:
  name: /EmailTool/GetJunkEmails
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/EmailTool/GetJunkEmails"
  headers:
    - name: userId
      value: garas@sasclearance.com
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: marinaplt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: "{{baseUrl}}/EmailTool/GetJunkEmails"
      method: GET
      headers:
        - name: userId
          value: <string>
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
