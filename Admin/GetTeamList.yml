info:
  name: GetTeamList
  type: http
  seq: 89

http:
  method: GET
  url: "{{BaseCoreUrl}}/Admin/GetTeamList"
  headers:
    - name: CompanyName
      value: marinapltq
    - name: UserToken
      value: "{{UserToken}}"
    - name: DepartmentId
      value: "3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
