info:
  name: /TaskMangerProject/ArchiveProject
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseCoreUrl}}/TaskMangerProject/ArchiveProject"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: periti
  body:
    type: multipart-form
    data:
      - name: Id
        type: text
        value: "5"
      - name: IsArchived
        type: text
        value: "true"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
