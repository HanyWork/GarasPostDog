info:
  name: "{{BaseCoreUrl}}/TaskManager/GetTaskListTest"
  type: http
  seq: 42

http:
  method: GET
  url: "{{BaseCoreUrl}}/TaskManager/GetTaskListTest"
  headers:
    - name: CompanyName
      value: garastest
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
