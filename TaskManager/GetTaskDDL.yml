info:
  name: GetTaskDDL
  type: http
  seq: 59

http:
  method: GET
  url: "{{BaseCoreUrl}}/TaskManager/GetTaskDDL"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: PERITI
    - name: ProjectId
      value: "1"
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
