info:
  name: reset
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5080/api/session/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
