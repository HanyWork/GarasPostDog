info:
  name: status
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5080/api/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
