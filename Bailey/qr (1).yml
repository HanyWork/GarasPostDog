info:
  name: qr (1)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5080/api/qr?format=ascii
  params:
    - name: format
      value: ascii
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
