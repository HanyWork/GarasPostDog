info:
  name: qr
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5080/api/qr/image
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
