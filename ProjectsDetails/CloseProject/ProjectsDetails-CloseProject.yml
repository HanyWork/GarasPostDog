info:
  name: /ProjectsDetails/CloseProject
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseCoreUrl}}/ProjectsDetails/CloseProject?projectId=50378"
  headers:
    - name: Accept
      value: text/plain
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: "{{CompanyName}}"
  params:
    - name: projectId
      value: "50378"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: "{{baseUrl}}/ProjectsDetails/CloseProject?projectId=<long>"
      method: POST
      headers:
        - name: Accept
          value: text/plain
      params:
        - name: projectId
          value: <long>
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "ID": "<long>",
            "Result": "<boolean>",
            "Errors": [
              {
                "ErrorMSG": "<string>",
                "ErrorCode": "<string>"
              },
              {
                "ErrorMSG": "<string>",
                "ErrorCode": "<string>"
              }
            ]
          }
