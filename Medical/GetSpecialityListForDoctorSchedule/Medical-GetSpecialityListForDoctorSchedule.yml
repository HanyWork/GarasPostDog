info:
  name: /Medical/GetSpecialityListForDoctorSchedule
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/Medical/GetSpecialityListForDoctorSchedule"
  headers:
    - name: DoctorID
      value: "156"
    - name: Accept
      value: text/plain
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: stmark
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: "{{baseUrl}}/Medical/GetSpecialityListForDoctorSchedule"
      method: GET
      headers:
        - name: Accept
          value: text/plain
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "DDLList": [
              {
                "ID": "<long>",
                "Name": "<string>"
              },
              {
                "ID": "<long>",
                "Name": "<string>"
              }
            ],
            "Result": "<boolean>",
            "Errors": [
              {
                "ErrorMSG": "<string>",
                "ErrorCode": "<string>"
              },
              {
                "ErrorMSG": "<string>",
                "ErrorCode": "<string>"
              }
            ]
          }
