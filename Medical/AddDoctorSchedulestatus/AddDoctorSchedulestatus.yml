info:
  name: AddDoctorSchedulestatus
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseCoreUrl}}/Medical/AddDoctorSchedulestatus"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: periti
  body:
    type: multipart-form
    data:
      - name: DoctorScheduleStatusType
        type: text
        value: test 02
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
