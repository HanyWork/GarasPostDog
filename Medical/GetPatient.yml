info:
  name: GetPatient
  type: http
  seq: 48

http:
  method: GET
  url: "{{BaseCoreUrl}}/Medical/GetPatient"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: stmark
    - name: PatientId
      value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
