info:
  name: AddNewPosNumber
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseCoreUrl}}/Medical/AddPosNumber"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: text/plain
    - name: CompanyName
      value: stmark
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: json
    data: "{\r\n    \"POSNumber\":\"123#\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
