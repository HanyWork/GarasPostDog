info:
  name: AddUsersAttedance44
  type: http
  seq: 34

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/AddUsersAttedance44"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
