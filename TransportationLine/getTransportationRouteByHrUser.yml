info:
  name: getTransportationRouteByHrUser
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/getTransportationRouteByHrUser"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: HrUserId
      value: "260"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
