info:
  name: ApproveTransportationLine
  type: http
  seq: 27

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/TransportationLine/ApproveTransportationLine"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: Id
      value: "1"
    - name: Approve
      value: "true"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
