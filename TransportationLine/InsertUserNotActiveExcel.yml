info:
  name: InsertUserNotActiveExcel
  type: http
  seq: 51

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/InsertUserNotActiveExcel"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: multipart-form
    data:
      - name: file
        type: text
        value: ""
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
