info:
  name: downloadExcelUserActiveTemplete
  type: http
  seq: 49

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/downloadExcelUserActiveTemplete"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
