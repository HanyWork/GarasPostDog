info:
  name: DeleteTransportationRoute
  type: http
  seq: 43

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/DeleteTransportationRoute"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: Id
      value: "10028"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
