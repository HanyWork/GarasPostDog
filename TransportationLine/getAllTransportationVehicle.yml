info:
  name: getAllTransportationVehicle
  type: http
  seq: 14

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/getAllTransportationVehicle"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: PageNo
      value: "1"
    - name: NoOfItems
      value: "2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
