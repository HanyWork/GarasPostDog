info:
  name: GetTransportationEmployeeList
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/GetTransportationEmployeeList"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: PageNo
      value: "1"
    - name: NoOfItems
      value: "2"
    - name: RouteId
      value: "1"
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
