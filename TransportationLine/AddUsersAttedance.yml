info:
  name: AddUsersAttedance
  type: http
  seq: 32

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/AddUsersAttedance44"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
