info:
  name: getTransportationRouteByUserId
  type: http
  seq: 58

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/getTransportationRouteByUserId"
  headers:
    - name: CompanyName
      value: transportion
    - name: UserToken
      value: "{{UserToken}}"
    - name: ParentId
      value: "10019"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
