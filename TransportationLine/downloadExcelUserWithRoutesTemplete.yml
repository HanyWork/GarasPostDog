info:
  name: downloadExcelUserWithRoutesTemplete
  type: http
  seq: 48

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/downloadExcelUserWithRoutesTemplete"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
