info:
  name: AddSupplierPayment
  type: http
  seq: 53

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/AddSupplierPayment"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: json
    data: |-
      {
        "SupplierId": 3,
        "Payment": 9000,
        "DatePayment": "2025-03-20",
        "StartDate": "2026-04-15",         // not Required
        "TypeOfDebt": "Advance" ,   // Advance  //Normal
        "NumberOfMonths": 3             // not Required
      }
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
