info:
  name: RejectUpdatePrice
  type: http
  seq: 43

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/RejectUpdatePrice"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: Id
      value: "1"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
