info:
  name: AddDeduction
  type: http
  seq: 36

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/AddDeduction"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
  body:
    type: json
    data: |-
      {
        "SupplierId": 3 ,
        "Serial":"70000001" ,
        "TransportationVehicleRouteId": 1,
        "DateOfDeduction": "2025-03-16T10:37:32.521Z",
        "DeductPerRound":50.00,
        "Cause": "ضرايب",
        "TypeOfDedct":"Advance"    //Advance   //Normal
      }
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
