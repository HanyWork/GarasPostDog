info:
  name: UpdatePriceOfTransportationLine
  type: http
  seq: 41

http:
  method: POST
  url: "{{BaseCoreUrl}}/api/Transportation/UpdatePriceOfTransportationLine"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: Id
      value: "3"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: CompanyName
      value: garastest

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
