info:
  name: GetTransportationDirection
  type: http
  seq: 7

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/GetTransportationDirection"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: routeId
      value: "10031"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
