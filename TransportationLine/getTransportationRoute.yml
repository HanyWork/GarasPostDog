info:
  name: getTransportationRoute
  type: http
  seq: 5

http:
  method: GET
  url: "{{BaseCoreUrl}}/api/Transportation/getTransportationRoute"
  headers:
    - name: CompanyName
      value: "{{CompanyName}}"
    - name: UserToken
      value: "{{UserToken}}"
    - name: RouteId
      value: "9"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
