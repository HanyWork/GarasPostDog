info:
  name: /ProjectLetterOfCredit/GetLetterOfCreditTypeDDL
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/ProjectLetterOfCredit/GetLetterOfCreditTypeDDL"
  headers:
    - name: UserToken
      value: "{{UserToken}}"
    - name: CompanyName
      value: "{{CompanyName}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
