info:
  name: /Vehicle/GetVehicleModel
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseCoreUrl}}/Vehicle/GetVehicleModel"
  headers:
    - name: VehicleBrandId
      value: "1"
    - name: Accept
      value: text/plain
    - name: CompanyName
      value: marinapltq
    - name: UserToken
      value: "{{UserToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: "{{baseUrl}}/Vehicle/GetVehicleModel"
      method: GET
      headers:
        - name: VehicleBrandId
          value: <long>
        - name: Accept
          value: text/plain
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
